class Position:
    def __init__(self, x: int, y: int) -> None:
        """
        Consruct a position object.

        `x` represents the x-axis in the coordinate system.
        `y` represents the y-axis in the coordinate system. 
        """
        self.x = x
        self.y = y
